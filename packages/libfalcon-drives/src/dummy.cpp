// Placeholder to ensure falcon_drives always has at least one source file,
// even when all optional features are disabled.

namespace falcon::drives {

int falcon_drives_placeholder_symbol() {
    return 0;
}

} // namespace falcon::drives
